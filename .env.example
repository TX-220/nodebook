# NodeBook — Chrome Extension
#
# This extension has no environment variables.
# All configuration is in source code (manifest.json, js/, css/).
# No .env file is needed to run or develop this extension.
#
# If you fork this project and add a server-side component,
# add your variables here and load them with dotenv.
